{
  "scripts": {
    "build": "esbuild main.js --outfile=public/main.js --bundle",
    "serve": "esbuild main.js --outfile=public/main.js --bundle --servedir=public"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "pngjs": "^6.0.0"
  }
}
